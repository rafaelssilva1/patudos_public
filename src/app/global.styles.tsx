"use client";

import globalStyles from "@base/theme/global.styles";
import { Global } from "@emotion/react";

const GlobalStyles = () => {
  return <Global styles={globalStyles} />;
};

export default GlobalStyles;
