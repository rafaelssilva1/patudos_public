import { css } from "@emotion/react";

export const boxShadow1 = css`
  box-shadow: 0rem 0.8rem 3.4rem 0rem var(--box-shadow);
`;

export const boxShadow2 = css`
  box-shadow: 0rem 0.2rem 0.2rem 0rem var(--box-shadow);
`;
